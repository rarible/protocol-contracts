diff --git a/node_modules/hardhat-deploy-immutable-proxy/dist/src/etherscan.js b/node_modules/hardhat-deploy-immutable-proxy/dist/src/etherscan.js
index 2cac72b..aa752c8 100644
--- a/node_modules/hardhat-deploy-immutable-proxy/dist/src/etherscan.js
+++ b/node_modules/hardhat-deploy-immutable-proxy/dist/src/etherscan.js
@@ -227,7 +227,9 @@ async function submitSources(hre, solcInputsPath, config) {
     async function submit(name, useSolcInput) {
         const deployment = all[name];
         const { address, metadata: metadataString } = deployment;
-        const abiResponse = await axios_1.default.get(`${host}/api?module=contract&action=getabi&address=${address}&apikey=${etherscanApiKey}`);
+        const abiBase = host.includes('/api') ? host : `${host}/api`;
+        const abiSep = abiBase.includes('?') ? '&' : '?';
+        const abiResponse = await axios_1.default.get(`${abiBase}${abiSep}module=contract&action=getabi&address=${address}&apikey=${etherscanApiKey}`);
         const { data: abiData } = abiResponse;
         let contractABI;
         if (abiData.status !== '0') {
@@ -355,8 +357,9 @@ async function submitSources(hre, solcInputsPath, config) {
             licenseType,
         };
         const formDataAsString = qs_1.default.stringify(postData);
+        const apiEndpoint = host.includes('/api') ? host : `${host}/api`;
         const submissionResponse = await axios_1.default.request({
-            url: `${host}/api`,
+            url: apiEndpoint,
             method: 'POST',
             headers: { 'content-type': 'application/x-www-form-urlencoded' },
             data: formDataAsString,
@@ -378,7 +381,9 @@ async function submitSources(hre, solcInputsPath, config) {
         }
         async function checkStatus() {
             // TODO while loop and delay :
-            const statusResponse = await axios_1.default.get(`${host}/api?apikey=${etherscanApiKey}`, {
+            const statusBase = host.includes('/api') ? host : `${host}/api`;
+            const statusSep = statusBase.includes('?') ? '&' : '?';
+            const statusResponse = await axios_1.default.get(`${statusBase}${statusSep}apikey=${etherscanApiKey}`, {
                 params: {
                     guid,
                     module: 'contract',
